class HotelModel {
  final String hotelName, countryTrans, city, address, imageUrl, bookingUrl;
  final dynamic reviewScore;

  HotelModel(this.hotelName, this.countryTrans, this.city, this.address, this.reviewScore, this.imageUrl, this.bookingUrl);
}